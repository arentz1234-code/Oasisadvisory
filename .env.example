# Google Calendar API Credentials
# Follow these steps to set up:
#
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project (or select existing)
# 3. Enable the Google Calendar API:
#    - Go to APIs & Services > Library
#    - Search for "Google Calendar API" and enable it
# 4. Create a Service Account:
#    - Go to APIs & Services > Credentials
#    - Click "Create Credentials" > "Service Account"
#    - Give it a name (e.g., "oasis-booking")
#    - Click "Create and Continue"
#    - Skip the optional steps
# 5. Create a key for the service account:
#    - Click on the service account you just created
#    - Go to "Keys" tab
#    - Click "Add Key" > "Create new key" > JSON
#    - Download the JSON file
# 6. Share your Google Calendar with the service account:
#    - Open Google Calendar
#    - Go to Settings > Settings for my calendars > [Your Calendar]
#    - Scroll to "Share with specific people"
#    - Add the service account email (ends with @...iam.gserviceaccount.com)
#    - Give it "Make changes to events" permission
# 7. Copy the values from the JSON file below:

GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=oasisadvisoryteam@gmail.com

# Admin Dashboard Password
# Change this to a secure password in production!
ADMIN_PASSWORD=oasis2024

# Upstash Redis Database
# Required for storing bookings on Vercel (serverless functions have read-only filesystem)
#
# Set up instructions:
# 1. Go to https://console.upstash.com and create an account
# 2. Click "Create Database"
# 3. Choose a name (e.g., "oasis-bookings") and region closest to you
# 4. Copy the REST URL and REST Token from the dashboard
# 5. Add these values to your Vercel Environment Variables:
#    - Go to your Vercel project > Settings > Environment Variables
#    - Add UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN
#
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token-here
